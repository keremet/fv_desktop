+Отмена выдачи/погашения
+Долг на дату
+Специальный тип данных вместо double (long long vs int и легкая смена между ними)
+Погашение
	+без пересчета графика
	+с пересчетом графика (отладить при автоматическом выносе на просрочку)
+Загрузка графика к выдаче (loadStatesFromCSV,привязать к платежу)
+Пропадание и игнорирование содержимого строки аннуитетного платежа при установке флажка 1 платеж
+заявка мб null при вводе участника
+Сумма платежа не может быть больше задолженности на дату - сделать проверку
+В главном окне отображать долг на текущую дату
+Выдача - в первый раз. Графики до выдачи не показывать.
+Удобство работы на нетбуке (окна под размер экрана 1024х900, экспорты - в ряд ниже).
+автоматическая смена даты выдачи договора сразу во всех 4-х календарях.
+при смене даты погашения меняется дата, на которую смотрится долг
+Убрать виды погашений
+автоматическое закрытие договора датой, когда долг стал=0
Расчет суммы к погашению к определенной дате (график+просрочка+проценты)


Баг одновременного создания 2-х последних платежей на договоре 39 (график заканчивается не на 0)
Вывод другой формы договора для случая возврата одним платежом
проверить совпадение дат закрытия и когда долг стал=0 на боевой базе
устранить ошибку смены типа участника (пользователь -> поручитель)

Сохранение договора без перезагрузки формы
Транзакционность
Добавление участников из формы договора


Стандартный календарь+edit при вводе участников

Загрузка графика к погашению

Учесть, что dateB и dateE могут быть в годах с разным кол-вом дней или между ними могут быть годы с разным кол-вом дней	

Регрессионные тесты выдач и погашений (Выдача в середине месяца, в конце, в начале, окончание в конце месяца	
	payment_log(agr_id, date, sum) = fv_payment ( часть полей )
	консольная погашалка (schedule -> payment_processing)
	сравнение эталонного и полученного содержимого БД ( echo "select..order by"|sqlite3; diff)
	Ошибка расчета платежа при: 31.08.13 - 30.09.14,  31.10.13 - 30.11.14 - платеж меньше, чем надо	
Отчет - договор без графика.

Дата выдачи = дате начала действия основной ставки
Обработка ошибок
фиксация заголовков гридов

Кнопка "Создать стандартный договор" с диалоговым окном по выбору даты, горячая клавиша на нее

Ограничение на ввод для числовых полей: только цифры, точки и запятые; в заявку - еще и минус

Шифрование базы
Выгрузка состояния в calc

Проверить, все ли функции и переменные используются
Коррекция структуры программы
	Base::xml загружается только 1 раз!

Написание правильного makefile

Статистика за период (месяц): количество выдач, погашений, участников, активных займов, сумма выдач и погашений, просрочка (сумма и длительность)

Красивая тема
